let num = 2.1234;
// // math라는 개체에 max라는 수식.
// let maxNum = Math.max(10, 5, 8, 30);
// let minNum = Math.min(10, 5, 8, 30);
// let roundNum = Math.round(num);
// let floorNum = Math.floor(num);
// let ceilNum = Math.ceil(num);
// let piNum = Math.PI(num);

// // 매개변수로 들어와있는 숫자중에서 가장 큰 값을 반환한다.
// document.write(maxNum, "<br />");
// // 매개변수로 들어와있는 숫자중에서 가장 작은 값을 반환한다.
// document.write(minNum, "<br />");
// // 매개변수로 들어와있는 숫자의 첫번째 소수점 자리수를 반올림하여 정수로 반환한다.
// document.write(roundNum, "<br />");
// // 매개변수로 들어와있는 숫자의 첫번째 소수점 자리수를 무조건 버림하여 정수로 반환한다.
// document.write(floorNum, "<br />");
// // 매개변수로 들어와있는 숫자의 첫번째 소수점 자리수를 무조건 올림하여 정수로 반환한다.
// document.write(ceilNum, "<br />");
// // 보통 인증번호 보낼 때 이 랜덤함수를 쓴다.
// document.write(rndNum, "<br />");
// // 0부터 1미만의 실수(*소수점을 가지고 있는 숫자)를 난수 형태로 반환.
// document.write(piNum, "<br />");
// // 원주율 상수를 반환


// 120-150사이의 정수를 랜덤값으로 추출하고 싶을 때(ex.로또추첨공식)
// +120은 120에서 150사이의 정수를 원해서 더한것임.
// document.write(Math.floor(Math.random() * 30) + 120);
